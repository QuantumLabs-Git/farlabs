# Far Node Server - GPU Provider Client Software
# This runs on user's machines with NVIDIA GPUs

# Core dependencies for distributed inference
# Note: Petals requires exact versions to avoid conflicts
git+https://github.com/bigscience-workshop/petals.git@main
hivemind>=1.1.10
transformers>=4.43.0
torch>=2.3.0
accelerate>=0.28.0
bitsandbytes==0.41.1

# API client and configuration
httpx>=0.27.0
python-dotenv>=1.0.1
pydantic>=2.6.0

# GPU monitoring
pynvml>=11.5.0
psutil>=5.9.0

# Optional: Payment tracking (lightweight)
web3>=6.15.1
